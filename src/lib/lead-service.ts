import { brand } from "@/config/brand";
export async function submitLead(data:unknown){if(!brand.leadWebhookUrl){if(process.env.NODE_ENV==="production")return {ok:false,error:"Le formulaire n’est pas encore connecté. Contactez-nous directement."};await new Promise(r=>setTimeout(r,500));console.info("[SIMULATION DEV] Lead validé",data);return {ok:true,simulated:true};}try{const res=await fetch(brand.leadWebhookUrl,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(data)});if(!res.ok)throw new Error(`Webhook: ${res.status}`);return {ok:true,simulated:false};}catch{return {ok:false,error:"La transmission a échoué. Contactez-nous par téléphone ou e-mail."};}}
