"use client";
import Link from "next/link";import { Menu,Phone,X } from "lucide-react";import { useState } from "react";import { brand } from "@/config/brand";import { Logo } from "./Logo";
const links=[{href:"/#services",label:"Services"},{href:"/professionnels",label:"Professionnels"},{href:"/particuliers",label:"Particuliers"},{href:"/reseau",label:"Notre réseau"},{href:"/qualite-securite",label:"Qualité"}];
export function Header(){const [open,setOpen]=useState(false);return <header className="header"><div className="nav-wrap"><Logo/><nav className="desktop-nav" aria-label="Navigation principale">{links.map(l=><Link key={l.href} href={l.href}>{l.label}</Link>)}</nav><div className="header-actions"><Link className="driver-link" href="/devenir-convoyeur">Devenir convoyeur</Link>{brand.phone&&<a className="btn secondary call-desktop" href={`tel:${brand.phone}`}><Phone size={17}/>Nous appeler</a>}<Link className="btn primary" href="/devis">Demander un devis</Link><button className="menu-button" onClick={()=>setOpen(!open)} aria-expanded={open} aria-controls="mobile-menu" aria-label={open?"Fermer le menu":"Ouvrir le menu"}>{open?<X/>:<Menu/>}</button></div></div>{open&&<nav id="mobile-menu" className="mobile-menu" aria-label="Navigation mobile">{links.map(l=><Link key={l.href} onClick={()=>setOpen(false)} href={l.href}>{l.label}</Link>)}<Link onClick={()=>setOpen(false)} href="/devenir-convoyeur">Devenir convoyeur</Link><Link onClick={()=>setOpen(false)} href="/contact">Contact</Link></nav>}</header>}
